#关于面向对象的新理解
面向抽象--将收到的数据抽象成一个“原型类”，正如java的object一样。例如：
将网络请求分成：header+body，由于是网络数据，body是一个byte数组，若通信双方遵循一样的传输格式（都用json），
那么服务端就可以还原请求数据。而此时，服务端不知道客户端将要传输什么数据，所以将客户端要传输的数据“统称”为proto，有利于建立
统一的数据接收接口。
不用考虑具体的实现细节，先将所有的细节抽象成一个（客户端会发送各种数据，但都被抽象成proto），通信数据满足proto格式，即可。
